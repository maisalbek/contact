import React from 'react';

const Props = (props) => {
    // console.log(props);
    const elem = 'five me them to App.js'
    // props.func(elem)

    return (
        <div>
            {/* {props.msg}
            <button onClick={()=>props.func(elem)}>Send elem</button> */}
        </div>
    );
};

export default Props;